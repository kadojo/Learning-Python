import random
import sys
import os

#Tuples
#Can't edit the contents
pi_tuple = (3,1,4,1,5,9)

new_tuple = list(pi_tuple)
new_list = tuple(new_tuple)

print(new_tuple)
print(new_list)
print(len(new_tuple))
print(min(new_tuple))
print(max(new_tuple))
